linters:
  disable-all: true
  enable:
    - lll
    - govet
    - gofmt

linters-settings:
  lll:
    line-length: 100
    tab-width: 4
  gofmt:
    simplify: false

issues:
  exclude-rules:
    - linters:
      - lll
      source: "\t?// (see )?https://"
