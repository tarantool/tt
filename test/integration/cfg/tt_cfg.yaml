tt:
  modules:
    directory: new_modules
  app:
    run_dir: /var/run
    log_dir: ./var/log
    wal_dir: lib/wal
    vinyl_dir: lib/vinyl
    memtx_dir: lib/memtx
  env:
    bin_dir: /usr/bin
    log_maxbackups: 12
