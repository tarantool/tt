tt:
  modules:
    directory: new_modules
  app:
    run_dir: /var/run
    log_dir: ./var/log
    log_maxbackups: 12
    wal_dir: lib/wal
    vinyl_dir: lib/vinyl
    memtx_dir: lib/memtx
    bin_dir: /usr/bin
